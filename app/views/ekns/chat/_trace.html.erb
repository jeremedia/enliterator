<div class="p-2 bg-gray-50 rounded text-xs text-gray-600 font-mono animate-pulse">
  <div class="font-semibold mb-1">🔍 Process Trace:</div>
  <% traces.each do |trace| %>
    <div class="ml-2 trace-line">
      <%= trace %>
    </div>
  <% end %>
</div>